<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="android:windowTranslucentStatus">true</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Toolbar.Theme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:actionMenuTextColor">@color/color_text_light</item>
        <item name="android:textColor">@color/color_text_light</item>
        <item name="android:textColorSecondary">@color/color_text_light</item>
    </style>

</resources>
